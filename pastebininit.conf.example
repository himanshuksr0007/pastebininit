# PastebinInit Configuration Template
# 
# Copy this file to ~/.pastebininit.conf to configure default values
# Each option below can be overridden via command-line arguments
#
# Example:
#   cp pastebininit.conf ~/.pastebininit.conf
#   nano ~/.pastebininit.conf
#
# Then use: pastebininit -f myfile.txt
# (will use values from config, override with -n, -l, -p flags as needed)

################################################################################
# Pastebin API Configuration
################################################################################

# Your Pastebin API key (optional for guest uploads)
# Get one at: https://pastebin.com/api
# WARNING: Never commit this to version control!
# API_KEY=""

################################################################################
# Default Paste Settings
################################################################################

# Default paste name format (uses filename if not specified)
# PASTE_NAME_PATTERN="$FILENAME - $(date '+%Y-%m-%d')"

# Default syntax highlighting language
# Common values: text, python, bash, javascript, java, c, cpp, sql, json, xml, yaml
DEFAULT_LANGUAGE="text"

# Default privacy level
# 0 = Public (visible to everyone)
# 1 = Unlisted (visible via direct link only)
# 2 = Private (only for you, requires API key)
DEFAULT_PRIVACY="0"

# Default expiration time
# N = Never, 10M = 10 minutes, 1H = 1 hour, 1D = 1 day
# 1W = 1 week, 2W = 2 weeks, 1M = 1 month
DEFAULT_EXPIRATION="N"

################################################################################
# Output Preferences
################################################################################

# Enable colored output (true/false)
USE_COLORS="true"

# Enable detailed success message (true/false)
SHOW_DETAILS="true"

# Show raw URL in addition to web URL (true/false)
SHOW_RAW_URL="true"

# Enable verbose mode by default (true/false)
VERBOSE="false"

################################################################################
# Performance Settings
################################################################################

# Connection timeout in seconds
CONNECTION_TIMEOUT="30"

# Maximum retry attempts for failed uploads
MAX_RETRIES="1"

# Retry delay in seconds
RETRY_DELAY="2"

################################################################################
# Logging
################################################################################

# Enable upload history logging (true/false)
LOG_UPLOADS="false"

# Log file path (if LOG_UPLOADS is true)
LOG_FILE="$HOME/.pastebininit.log"

# Keep logs for X days (0 = keep all)
LOG_RETENTION_DAYS="90"

################################################################################
# Advanced Options
################################################################################

# Proxy settings (leave empty if not using proxy)
# HTTP_PROXY=""
# HTTPS_PROXY=""

# Custom Pastebin API endpoint (usually not needed)
# PASTEBIN_API_URL="https://pastebin.com/api/api_post.php"

# Disable HTTPS verification (NOT RECOMMENDED for security)
# INSECURE_MODE="false"
